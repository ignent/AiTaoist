services:
  aitaoist:
    build: .
    container_name: aitaoist
    restart: unless-stopped
    ports:
      - "${PORT:-9999}:9999"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 9999
